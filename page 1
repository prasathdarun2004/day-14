#include <stdio.h>

int main()
{
    int total,page,m;
    scanf("%d %d",&total,&page);
    m=total/2-page/2;
    if(m<=page/2)
    {
        printf("%d",m);
    }
    else {
        printf("%d",page/2);
    }
    

    return 0;
}
